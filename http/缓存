1 用时间来比较、
要把最新的修改时间拿出来 返回给浏览器  last-Modified
读出来 if-Modified-Since 和最新的时间作比较

缺点 不精确 可能获取不到 浏览器可能获取不到


2比较内容
把最新的内容的md5拿出来 返回给浏览器 Etag
读出来 if-none-match 和最新的内容生成md5比较

缺点 必须将文件全部读出

最快的  不发请求

如果有变化 返回最新的东西的文件